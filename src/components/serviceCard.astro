---
import { Image } from "astro:assets";

const { img, title, description } = Astro.props;
---

<article>
    <img src={img} alt={title} />
    <div class="content">
        <h3>{title}</h3>
        <p>{description}</p>
    </div>
</article>

<style>
    article {
        width: 400px;
        border: 1px solid #00000023;
        border-radius: 1rem;
        box-shadow: 0 5px 5px 5px #adadad54;
    }

    img {
        height: 15rem;
        width: 100%;
        border-radius: 1rem;
        object-fit: cover;
    }

    .content {
        margin: 0 30px;
    }

    h3 {
        color: var(--primary-yellow);
        margin-bottom: 10px;
    }

    p {
        margin-bottom: 10px;
    }
</style>
